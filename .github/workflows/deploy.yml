name: Deploy CrowAutomobile to Hostinger Temp Domain

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'  # change if your Hostinger is 8.2 or 8.4
          extensions: mbstring,bcmath,intl,pdo_mysql,gd,zip,curl,exif,pcntl,redis

      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install & build assets
        run: |
          npm ci
          npm run build

      - name: Install sshpass
        run: sudo apt-get update && sudo apt-get install -y sshpass

      - name: Deploy files with rsync
        run: |
          sshpass -p "${{ secrets.SSH_PASSWORD }}" rsync -avz --delete \
            -e "ssh -p 65002 -o StrictHostKeyChecking=no" \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='node_modules' \
            --exclude='vendor' \
            --exclude='.env' \
            --exclude='storage/logs/*' \
            --exclude='storage/framework/cache/*' \
            --exclude='storage/framework/sessions/*' \
            --exclude='storage/framework/views/*' \
            ./ u763117446@147.93.78.59:/home/u763117446/domains/lightslategrey-mandrill-987797.hostingersite.com/public_html/

      - name: Run artisan commands on server
        run: |
          sshpass -p "${{ secrets.SSH_PASSWORD }}" ssh -p 65002 -o StrictHostKeyChecking=no u763117446@147.93.78.59 << 'EOF'
            cd /home/u763117446/domains/lightslategrey-mandrill-987797.hostingersite.com/public_html/

            /opt/alt/php83/usr/bin/php /usr/local/bin/composer2 install --no-dev --optimize-autoloader --no-interaction || true

            /opt/alt/php83/usr/bin/php artisan optimize:clear

            /opt/alt/php83/usr/bin/php artisan storage:link --force || true

            /opt/alt/php83/usr/bin/php artisan config:cache
            /opt/alt/php83/usr/bin/php artisan route:cache || true
            /opt/alt/php83/usr/bin/php artisan view:cache || true

            # Optional: auto-migrate (be careful!)
            # /opt/alt/php83/usr/bin/php artisan migrate --force --no-interaction
          EOF